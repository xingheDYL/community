<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dyl.community.mapper.DiscussPostMapper">

    <resultMap id="BaseResultMap" type="DiscussPost">
        <id property="id" column="id" jdbcType="INTEGER"/>
        <result property="userId" column="user_id" jdbcType="VARCHAR"/>
        <result property="title" column="title" jdbcType="VARCHAR"/>
        <result property="content" column="content" jdbcType="VARCHAR"/>
        <result property="type" column="type" jdbcType="INTEGER"/>
        <result property="status" column="status" jdbcType="INTEGER"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="commentCount" column="comment_count" jdbcType="INTEGER"/>
        <result property="score" column="score" jdbcType="DOUBLE"/>
    </resultMap>

    <!--    <sql id="Base_Column_List">-->
    <!--        id,user_id,title,-->
    <!--        content,type,status,-->
    <!--        create_time,comment_count,score-->
    <!--    </sql>-->

    <!--    &lt;!&ndash;Page<DiscussPost> selectPage(@Param("page") Page<DiscussPost> page, @Param("userId") Integer userId)&ndash;&gt;-->
    <!--    <select id="selectPage" resultType="DiscussPost">-->
    <!--        select-->
    <!--        <include refid="Base_Column_List"/>-->
    <!--        from discuss_post-->
    <!--        where status != 2-->
    <!--        <if test="userId!=0">-->
    <!--            and user_id = #{userId}-->
    <!--        </if>-->
    <!--        order by type desc, create_time desc-->
    <!--    </select>-->


    <sql id="selectFields">
        id, user_id, title, content, type, status, create_time, comment_count, score
    </sql>

    <sql id="insertFields">
        user_id, title, content, type, status, create_time, comment_count, score
    </sql>

    <select id="selectDiscussPosts" resultType="DiscussPost">
        select <include refid="selectFields"></include>
        from discuss_post
        where status != 2
        <if test="userId!=0">
            and user_id = #{userId}
        </if>
        order by type desc, create_time desc
        limit #{offset}, #{limit}
    </select>

<!--    <select id="selectDiscussPosts" resultType="DiscussPost">-->
<!--        select-->
<!--        <include refid="selectFields"/>-->
<!--        from discuss_post-->
<!--        where status != 2-->
<!--        <if test="userId!=0">-->
<!--            and user_id = #{userId}-->
<!--        </if>-->
<!--        <if test="orderMode==0">-->
<!--            order by type desc, create_time desc-->
<!--        </if>-->
<!--        <if test="orderMode==1">-->
<!--            order by type desc, score desc, create_time desc-->
<!--        </if>-->
<!--        limit #{offset}, #{limit}-->
<!--    </select>-->

    <select id="selectDiscussPostRows" resultType="int">
        select count(id)
        from discuss_post
        where status != 2
        <if test="userId!=0">
            and user_id = #{userId}
        </if>
    </select>

    <insert id="insertDiscussPost" parameterType="DiscussPost" keyProperty="id">
        insert into discuss_post(<include refid="insertFields"/>)
        values(#{userId},#{title},#{content},#{type},#{status},#{createTime},#{commentCount},#{score})
    </insert>

    <select id="selectDiscussPostById" resultType="DiscussPost">
        select
        <include refid="selectFields"/>
        from discuss_post
        where id = #{id}
    </select>

    <update id="updateCommentCount">
        update discuss_post set comment_count = #{commentCount} where id = #{id}
    </update>

    <update id="updateType">
        update discuss_post set type = #{type} where id = #{id}
    </update>

    <update id="updateStatus">
        update discuss_post set status = #{status} where id = #{id}
    </update>

    <update id="updateScore">
        update discuss_post set score = #{score} where id = #{id}
    </update>
</mapper>
